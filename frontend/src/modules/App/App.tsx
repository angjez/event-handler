import AddEvent from "@src/modules/AddEvent";
import { Wrapper } from "@src/modules/App/App.components";
import React, { FunctionComponent } from "react";

const App: FunctionComponent = () => (
  <Wrapper>
    <AddEvent />
  </Wrapper>
);

export default App;
